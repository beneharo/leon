/*
 * JS for Login generated by Appery.io
 *
 */

Appery.getProjectGUID = function() {
    return 'e7466853-2ac3-48d5-a5d1-6bf491ec4579';
}

function navigateTo(outcome, useAjax) {
    Appery.navigateTo(outcome, useAjax);
}

function adjustContentHeight() {
    Appery.adjustContentHeight();
}

function adjustContentHeightWithPadding() {
    Appery.adjustContentHeightWithPadding();
}

function setDetailContent(pageUrl) {
    Appery.setDetailContent(pageUrl);
}

Appery.AppPages = [{
    "name": "Home",
    "location": "Home.html"
}, {
    "name": "Splash",
    "location": "Splash.html"
}, {
    "name": "Busqueda",
    "location": "Busqueda.html"
}, {
    "name": "Eventos",
    "location": "Eventos.html"
}, {
    "name": "Calendario",
    "location": "Calendario.html"
}, {
    "name": "Configuracion",
    "location": "Configuracion.html"
}, {
    "name": "Perfil",
    "location": "Perfil.html"
}, {
    "name": "Amigos",
    "location": "Amigos.html"
}, {
    "name": "Login",
    "location": "Login.html"
}];

Login_js = function(runBeforeShow) { /* Object & array with components "name-to-id" mapping */
    var n2id_buf = {
        'user_image': 'Login_user_image',
        'lb_usuario': 'Login_lb_usuario',
        'in_usuario': 'Login_in_usuario',
        'lb_password': 'Login_lb_password',
        'in_password': 'Login_in_password',
        'lb_recordarPass': 'Login_lb_recordarPass',
        'bt_entrar': 'Login_bt_entrar'
    };

    if ("n2id" in window && window.n2id !== undefined) {
        $.extend(n2id, n2id_buf);
    } else {
        window.n2id = n2id_buf;
    }

    if (navigator.userAgent.indexOf("IEMobile") != -1) {
        //Fixing issue https://github.com/jquery/jquery-mobile/issues/5424 on Windows Phone
        $("div[data-role=page] div[data-role=footer]").css("bottom", "-36px");
    }

    if (Appery.getTargetPlatform == "I") {
        $.each(window.n2id, function(name, id) {
            var elApperyRole = $("#" + id).attr("data-role");
            if (elApperyRole != "appery_label" && elApperyRole != "appery_link") {
                var appleDataSelector = "#" + id + "[x-apple-data-detectors], #" + id + " [x-apple-data-detectors]";
                $(appleDataSelector).attr("x-apple-data-detectors", "false");
            }
        });
    }

    Appery.CurrentScreen = 'Login';

    /*
     * Nonvisual components
     */
    var datasources = [];

    /*
     * Events and handlers
     */

    // Before Show
    Login_beforeshow = function() {
        Appery.CurrentScreen = "Login";
        for (var idx = 0; idx < datasources.length; idx++) {
            datasources[idx].__setupDisplay();
        }
    }

    // On Load
    screen_D592_onLoad = Login_onLoad = function() {
        screen_D592_elementsExtraJS();

        // TODO fire device events only if necessary (with JS logic)
        Login_deviceEvents();
        Login_windowEvents();
        screen_D592_elementsEvents();
    }

    // screen window events
    screen_D592_windowEvents = Login_windowEvents = function() {

        $('#Login').bind('pageshow orientationchange', function() {
            adjustContentHeightWithPadding();
        });

    }

    // device events
    Login_deviceEvents = function() {

        document.addEventListener("deviceready", function() {

        });
    }

    // screen elements extra js
    screen_D592_elementsExtraJS = Login_elementsExtraJS = function() {
        // screen (Login) extra code

    }

    // screen elements handler
    screen_D592_elementsEvents = Login_elementsEvents = function() {

        $("a :input,a a,a fieldset label").live({
            click: function(event) {
                event.stopPropagation();
            }
        });

        $('#Login_mobilecontainer [name="in_usuario"]').die().live({
            change: function() {
                localStorage.setItem('usuario', '');
            },
        });

        $('#Login_mobilecontainer [name="in_password"]').die().live({
            change: function() {
                localStorage.setItem('password', '');
            },
        });

        //$('#Login_mobilecontainer [name="bt_entrar"]').die().live({
           // click: function() {
             //   if (!$(this).attr('disabled')) {
             //       Appery.navigateTo('Home', {
            //            transition: 'slide',
            //            reverse: false
             //       });

              //  }
           // },
       // });

    }

    $("#Login").die("pagebeforeshow").live("pagebeforeshow", function(event, ui) {
        Login_beforeshow();
    });

    if (runBeforeShow) {
        Login_beforeshow();
    } else {
        Login_onLoad();
    }

}

$("#Login").die("pageinit").live("pageinit", function(event, ui) {
    Appery.processSelectMenu($(this));
    Login_js();
});
